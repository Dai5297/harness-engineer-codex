# Project-scoped Codex subagent settings.
# Keep global subagent limits here so the repo can recommend a predictable delegation shape.

sandbox_mode = "read-only"

[agents]
max_threads = 6
max_depth = 1
