# 前端指南

## 这里应该记录什么

- 路由归属与页面边界
- 共享 UI 模式与交互规则
- 状态管理约定
- 可访问性与响应式要求

## 默认规则

1. 展示组件应聚焦渲染与交互。
2. 数据获取、策略判断与数据变换应尽量靠近功能边界。
3. 只有在确实传达同一种含义时才复用模式。
4. 加载、错误和空状态都是功能的一部分，不是边角情况。

## 什么时候更新这个文件

当某条前端规则应当适用于多个功能，而不只是单个 PR 时，就更新这里。

